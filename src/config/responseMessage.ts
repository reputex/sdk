const responseMessage = {
  EMPTY_ACCESS_OR_SECRET_KEY:
    "The access key and secret key cannot be empty or null.",
  INVALID_INPUT: "The value provided cannot be empty or null.",
};

export default responseMessage;
